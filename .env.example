# JWT Secret (минимум 32 символа)
JWT_SECRET=your_jwt_secret_here_min_32_chars

# Encryption Key (Base64 encoded 256-bit key)
# Generate with: java -cp build/libs/bankcards-*.jar ru.effectivemobile.bankcards.util.KeyGeneratorUtil
ENCRYPTION_KEY=your_base64_encoded_256_bit_key_here